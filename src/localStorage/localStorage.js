/* 
    Se buscará en el local storage y, en caso de existir una session abierta, 
    se tomarán los datos del local y se enviaran a redux. De lo contrario se 
    tomarán los datos del redux para enviar al local y mantener la session abierta.
*/

const checkInLocalStorage = () => {

}